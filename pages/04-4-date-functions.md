## 학습 목표

- 날짜형 데이터를 다루는 기본 함수를 사용할 수 있습니다.
- 날짜 차이, 날짜 추출, 날짜 더하기 개념을 이해할 수 있습니다.

## 목차

1. 날짜형 함수의 역할
2. 자주 쓰는 함수

## 1. 날짜형 함수의 역할

날짜형 함수는 연도, 월, 일 추출이나 기간 계산처럼 시계열 분석의 기본 작업에 필요합니다.

## 2. 자주 쓰는 함수

- `CURRENT_DATE`
- `DATE_ADD`
- `DATEDIFF`
- `EXTRACT`
- `DATE_TRUNC`
