조회와 필터링의 기본 문법을 실무 관점으로 정리하는 챕터입니다.

## 이 챕터의 목표

- SELECT, DISTINCT, WHERE, ORDER BY, LIMIT를 정확히 사용할 수 있습니다.
- NULL 비교와 조건식 평가 방식을 이해할 수 있습니다.
- 날짜, 문자열, 숫자 필터에서 자주 생기는 오류를 줄일 수 있습니다.

## 세부 목차

- SELECT와 DISTINCT
- WHERE 조건식과 논리 연산자
- ORDER BY와 LIMIT
- NULL, LIKE, BETWEEN, IN
