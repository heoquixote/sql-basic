SQL 안에서 조건에 따라 값을 다르게 반환하는 방법을 다루는 챕터입니다.

## 이 챕터의 목표

- 조건문이 왜 필요한지 이해할 수 있습니다.
- `CASE WHEN` 문을 사용해 값을 분기할 수 있습니다.
- 분류, 라벨링, 구간화에 조건문을 적용할 수 있습니다.

## 세부 목차

- 조건문이 필요한 이유
- `CASE WHEN` 기본 문법
- 실무에서 자주 쓰는 분기 패턴
