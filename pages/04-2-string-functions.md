## 학습 목표

- 문자열 데이터를 가공하는 대표 함수를 사용할 수 있습니다.
- 문자열 전처리에서 어떤 함수가 필요한지 판단할 수 있습니다.

## 목차

1. 문자열 함수의 역할
2. 자주 쓰는 함수

## 1. 문자열 함수의 역할

문자열 함수는 텍스트 데이터를 자르거나 붙이고, 길이를 계산하고, 대소문자를 바꾸는 데 사용합니다.

## 2. 자주 쓰는 함수

- `CONCAT`
- `SUBSTRING`
- `LENGTH`
- `UPPER`, `LOWER`
- `TRIM`
