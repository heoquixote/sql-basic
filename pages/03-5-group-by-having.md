## 학습 목표

- 데이터를 그룹 단위로 묶을 수 있습니다.
- 그룹 결과에 조건을 걸 수 있습니다.

## 목차

1. GROUP BY
2. HAVING
3. WHERE와 HAVING의 차이

## 1. GROUP BY

`GROUP BY`는 여러 행을 특정 기준으로 묶어 집계할 때 사용합니다.

## 2. HAVING

`HAVING`은 그룹화된 결과에 조건을 거는 절입니다.

## 3. WHERE와 HAVING의 차이

- `WHERE`: 그룹화 전, 행 단위 필터
- `HAVING`: 그룹화 후, 그룹 단위 필터
