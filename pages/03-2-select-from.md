## 학습 목표

- `SELECT ~ FROM`의 기본 구조를 이해할 수 있습니다.
- 필요한 컬럼만 선택해서 조회할 수 있습니다.

## 목차

1. SELECT의 역할
2. FROM의 역할
3. 기본 조회 예시

## 1. SELECT의 역할

`SELECT`는 어떤 컬럼을 보여줄지 결정하는 절입니다.

## 2. FROM의 역할

`FROM`은 어떤 테이블에서 데이터를 가져올지 지정하는 절입니다.

## 3. 기본 조회 예시

- 전체 컬럼 조회
- 일부 컬럼만 조회
- 컬럼 별칭 사용
