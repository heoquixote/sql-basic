## 학습 목표

- `WHERE`를 사용해 원하는 행만 남길 수 있습니다.
- 비교 연산자와 논리 연산자를 함께 사용할 수 있습니다.

## 목차

1. WHERE란?
2. 비교 연산자
3. 논리 연산자

## 1. WHERE란?

`WHERE`는 행 단위 조건을 걸어 데이터를 필터링하는 절입니다.

## 2. 비교 연산자

- `=`, `!=`, `>`, `>=`, `<`, `<=`
- `BETWEEN`, `IN`, `LIKE`

## 3. 논리 연산자

- `AND`
- `OR`
- `NOT`
